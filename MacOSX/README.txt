Mac OSX Users
-------------

Copy the file statesaver.so into the subdirectory bub-n-bros/bubbob.

It has been compiled for Python 2.2 but should run fine with Python 2.3 as 
well.

Note that you must still install separately Python and pygame.
